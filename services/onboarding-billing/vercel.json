{
	"framework": "nextjs",
	"installCommand": "cd ../.. && npm ci && cd services/onboarding-billing && npm ci",
	"buildCommand": "npm run build",
	"headers": [
		{
			"source": "/api/(.*)",
			"headers": [
				{ "key": "X-Service", "value": "onboarding-billing" }
			]
		}
	]
}
