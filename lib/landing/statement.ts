import { LANDING_PRODUCT_NAME } from "./brand";

export const LANDING_STATEMENT_TEXT =
	`${LANDING_PRODUCT_NAME} convierte cada pedido en una relación directa entre tu restaurante y tu cliente — sin intermediarios ni comisiones.`;

export const LANDING_STATEMENT_ACCENT_START = LANDING_STATEMENT_TEXT.indexOf("sin intermediarios");
