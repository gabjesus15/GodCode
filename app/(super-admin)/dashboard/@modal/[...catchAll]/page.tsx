/** Cualquier otra página de /dashboard (pagos, auditoría…) va sin ventana: al navegar hacia ella
 * la ventana abierta se cierra en vez de quedar encima. */
export default function NoModalCatchAll() {
	return null;
}
