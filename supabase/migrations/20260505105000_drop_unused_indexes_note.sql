-- Índices "unused" del advisor: ejecutar DROP INDEX CONCURRENTLY solo tras validar
-- pg_stat_user_indexes / advisor durante 7+ días en producción. No se listan nombres aquí
-- porque dependen del historial de cada proyecto.

SELECT 1;
