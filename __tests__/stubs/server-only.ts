/**
 * Stub de `server-only` para vitest.
 *
 * El paquete real lanza al importarse fuera de un Server Component, lo que impide
 * testear cualquier módulo marcado como server-only. Aliasado solo en
 * `vitest.config.ts`: la guarda sigue intacta en el build de Next.
 */
export {};
